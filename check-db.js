const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function checkConfigs() { const configs = await prisma.savedConfiguration.findMany({ select: { id: true, name: true, createdAt: true } }); console.log('📊 Database configurations:'); configs.forEach(c => console.log(`- ID: ${c.id}, Name: ${c.name}, Created: ${c.createdAt}`)); await prisma.$disconnect(); } checkConfigs().catch(console.error);
